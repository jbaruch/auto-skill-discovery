# Run the A1 Batch on a Small Attendee CSV

## Problem/Feature Description

Run the batch-driver skill on `inputs/attendees.csv`. The CSV has three companies; produce per-company artifacts and a batch summary.

## Output Specification

Produce the canonical batch artifacts: one shared run directory containing batch-manifest.json, per-company subdirectories, and an index.md summarizing outcomes.
