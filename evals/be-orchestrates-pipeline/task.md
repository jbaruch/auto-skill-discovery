# Build and Evaluate the Selected Skill

## Problem/Feature Description

A selection.json is at `inputs/selection.json` and discovery.json at `inputs/discovery.json`. Run the build-and-evaluate pipeline (phases 4–10).

## Output Specification

Produce the canonical per-run artifacts (generated skill, scenarios, review JSON, eval results, lift JSON, gap analysis, final report) using the tile-prescribed command sequence.
