# Run Build-and-Evaluate

## Problem/Feature Description

A selection.json is at `inputs/selection.json` and the linked discovery.json is at `inputs/discovery.json`. Run the build-and-evaluate pipeline.
