# Consume-Mode Discovery on a Small Pre-Product Startup

## Problem/Feature Description

Run consume-mode discovery on "Asparanta" — a small company with no public engineering blog, no GitHub org, no published talks, no hiring posts describing internal tooling. The marketing site mentions AI but does not reveal any internal stack the engineering team consumes daily.

## Output Specification

Write the discovery JSON to its versioned run directory and pass the contract validator.
