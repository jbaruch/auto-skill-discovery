# Consume-Mode Discovery on Asparanta

## Problem/Feature Description

Run consume-mode discovery on Asparanta. Persist the discovery JSON to its versioned run directory and pass the contract validator.
