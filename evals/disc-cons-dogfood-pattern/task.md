# Consume-Mode Discovery on Anysphere (Cursor)

## Problem/Feature Description

Run consume-mode discovery on Anysphere (the company behind Cursor). Persist the discovery JSON to its versioned run directory and pass the contract validator.
