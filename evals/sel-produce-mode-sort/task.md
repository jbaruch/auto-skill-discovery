# Present Target Candidates from a Produce-Mode Discovery

## Problem/Feature Description

A discovery.json is at `inputs/discovery.json`. Run target selection: present the human with the candidates for review and wait for their pick.

## Output Specification

Present the candidates in a markdown table and ask the human to reply with a target ID, `skip`, or `defer`.
