# Target Selection for Novaheim

## Problem/Feature Description

A discovery pipeline run has completed for Novaheim. The output is available at `inputs/discovery.json`. A sales engineer needs to know whether there are any viable engineering targets to pursue and what the next steps should be.

Run the target selection workflow on this discovery output and write the results to `selection-output.md`.

## Output Specification

- `selection-output.md` — a markdown file containing the full output produced by running the target selection workflow on the discovery file.
